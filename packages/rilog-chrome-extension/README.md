# rilog-chrome-extension

Placeholder package. Will hold the Chrome (Manifest V3) DevTools extension: standalone event capture via a `MAIN`-world content script built on `@rilog-development/rilog-shared`, a DevTools panel, a rules engine, and a Share adapter.

Not yet scaffolded — build tooling (`vite` + `@crxjs/vite-plugin`), manifest, and source structure are a later phase.
