{
  "name": "rilog",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@rilog-development/rilog-lib",
    "test": "npm run test --workspace=@rilog-development/rilog-lib",
    "lint": "npm run lint --workspace=@rilog-development/rilog-lib"
  }
}
